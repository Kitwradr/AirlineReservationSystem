TO select all ticket id from user id:

Select FLIGHT
from ticket
where username=?

TO ADD TICKET :

INSERT INTO TICKET
(FLIGHT,
TIMEOFBOOKING,
USERNAME,
PAYMENTSTATUS,
PRICE)
VALUES
(?,DATETIME('NOW'),?,?,?)

TO RETREIVE FLIGHT DETAILS FROM TICKETID

SELECT * 
FROM FLIGHT,TICKET
WHERE FLIGHTID=TICKET.FLIGHT AND USERNAME=?